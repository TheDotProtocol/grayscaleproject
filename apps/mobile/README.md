# Project Grayscale — Mobile (Flutter)

## Prerequisites

Install Flutter SDK:

```bash
brew install --cask flutter
flutter doctor
```

## Run

```bash
cd apps/mobile
flutter pub get
flutter run
```

## API

Point to backend: `http://localhost:4000/api` (configure in lib/config.dart when added).

Feature parity with web is maintained through shared REST API contracts.
