# Scenario Planning

**Sprint 3 Phase C**

Scenarios consume the Living Organizational Twin. Each scenario defines assumptions and constraints applied to a twin version without modifying reality.

Supported scenario types align with `SimulationScenarioType` in `@grayscale/platform/simulation`.

Council decision classes requiring simulation (`innovation`, `growth`, `partnership`, `investment`, `acquisition`) may reference simulation session IDs as evidence.
